@{
    ViewData["Title"] = "İlanlarımı Yönet";
}

<div class="card shadow mb-4">
    <div class="card-header py-3 d-flex justify-content-between align-items-center">
        <h6 class="m-0 font-weight-bold text-primary">Eklediğim İlanlar</h6>
        <a href="/Properties/Create" class="btn btn-sm btn-success"><i class="fas fa-plus"></i> Yeni İlan Ekle</a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-bordered" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>Başlık</th>
                        <th>Durum</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>
                <tbody id="myPropertiesList">
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            loadMyProperties();

            function loadMyProperties() {
                var token = localStorage.getItem("token");
                $.ajax({
                    url: "http://localhost:5155/api/Properties/MyProperties",
                    type: "GET",
                    beforeSend: function (xhr) {
                        xhr.setRequestHeader("Authorization", "Bearer " + token);
                    },
                    success: function (data) {
                        var html = "";
                        $.each(data, function (index, item) {
                            html += `<tr>
                                        <td>${item.title}</td>
                                        <td><span class="badge badge-success">${item.statusName}</span></td>
                                        <td>
                                        <a href="/Properties/Edit/${item.propertyId}" class="btn btn-warning btn-sm mr-2">
                                                <i class="fas fa-edit"></i> Düzenle
                                            </a>
                                            <button onclick="deleteProperty(${item.propertyId})" class="btn btn-danger btn-sm">
                                                <i class="fas fa-trash"></i> Sil
                                            </button>
                                        </td>
                                    </tr>`;
                        });
                        $("#myPropertiesList").html(html);
                    },
                    error: function() {
                        alert("İlanlarınız yüklenirken bir hata oluştu.");
                    }
                });
            }
        });

        function deleteProperty(id) {
            if (confirm("Bu ilanı silmek istediğinize emin misiniz?")) {
                var token = localStorage.getItem("token");
                $.ajax({
                    url: "http://localhost:5155/api/Properties/" + id,
                    type: "DELETE",
                    beforeSend: function (xhr) {
                        xhr.setRequestHeader("Authorization", "Bearer " + token);
                    },
                    success: function () {
                        alert("İlan başarıyla silindi.");
                        location.reload();
                    }
                });
            }
        }
    </script>
}